---
import { getCollection } from 'astro:content'
import BaseLayout from '../../layouts/BaseLayout.astro'

const posts = (
  await getCollection(
    'blog',
    ({ data }) => !data.draft && !data.hiddenFromIndex,
  )
).sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())

const shortDate = new Intl.DateTimeFormat('en-US', {
  month: 'short',
  day: 'numeric',
  year: 'numeric',
  timeZone: 'UTC',
})

const entries = [
  ...posts.map((post) => ({
    title: post.data.title,
    displayDate: shortDate.format(post.data.date),
    date: post.data.date,
    href: `/blog/${post.id}/`,
    tag: (post.data.topics[0] ?? 'post').toLowerCase(),
  })),
  {
    title: 'Bellman Equation',
    displayDate: shortDate.format(new Date('2026-09-16')),
    date: new Date('2026-09-16'),
    href: '/notes/reinforcement-learning/',
    tag: 'RL',
  },
].sort((a, b) => b.date.valueOf() - a.date.valueOf())

const filters = ['all', 'RL', 'online learning']

---

<BaseLayout
  title="Blog"
  description="Blog posts by Jungbin Jun."
  activePage="blog"
>
  <div class="blog-page blog-index-page">
    <div class="blog-hero blog-index-hero">
      <h1>blog</h1>
    </div>

    <div class="post-filters" role="group" aria-label="Filter posts by tag">
      {
        filters.map((filter, index) => (
          <button
            type="button"
            class:list={['post-filter', { active: index === 0 }]}
            data-filter={filter.toLowerCase()}
            aria-pressed={index === 0 ? 'true' : 'false'}
          >
            {filter}
          </button>
        ))
      }
    </div>

    <div class="post-feed" aria-live="polite">
      {
        entries.map((entry) => (
          <article
            class="post-feed-item"
            data-post-item
            data-tag={entry.tag.toLowerCase()}
          >
            <time datetime={entry.date.toISOString()}>{entry.displayDate}</time>
            <div class="post-feed-body">
              <span class="post-feed-tag">{entry.tag}</span>
              <h2><a href={entry.href}>{entry.title}</a></h2>
            </div>
          </article>
        ))
      }
      <p class="post-feed-empty" data-empty-state hidden>
        No posts in this topic yet.
      </p>
    </div>
  </div>

  <script>
    const filterButtons = document.querySelectorAll('[data-filter]')
    const postItems = document.querySelectorAll('[data-post-item]')
    const emptyState = document.querySelector('[data-empty-state]')

    filterButtons.forEach((button) => {
      button.addEventListener('click', () => {
        if (!(button instanceof HTMLButtonElement)) return
        const filter = button.dataset.filter || 'all'
        let visibleCount = 0

        filterButtons.forEach((item) => {
          const active = item === button
          item.classList.toggle('active', active)
          item.setAttribute('aria-pressed', active ? 'true' : 'false')
        })

        postItems.forEach((item) => {
          if (!(item instanceof HTMLElement)) return
          const visible = filter === 'all' || item.dataset.tag === filter
          item.hidden = !visible
          if (visible) visibleCount += 1
        })

        if (emptyState instanceof HTMLElement) {
          emptyState.hidden = visibleCount !== 0
        }
      })
    })
  </script>
</BaseLayout>
