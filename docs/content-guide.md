# Content editing guide

Edit profile and publication data in `src/data/siteData.js`.

Add blog posts as Markdown files under `src/content/blog/`. Astro creates the
post page and blog index entry automatically from the frontmatter.

Run `npm run check` before pushing.
